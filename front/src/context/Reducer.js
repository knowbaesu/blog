const Reducer = (state, action) => {
    switch (action.type) {
        case "Login_start":
            return {
                user:null,
                insFetching:true,
                error:false
            };
            case "Login_start":
            return {
                user:null,
                insFetching:true,
                error:false
            
        }
        case "Login_start":
            return {
                user:null,
                insFetching:true,
                error:false
            }
            default:
                return  state;
    }
}